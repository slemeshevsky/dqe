pipeline {
   agent any
      stages {
         stage('Build') {
            steps {
               sh 'python3 -m ensurepip --upgrade'
               sh 'python3 -m pip install -r requirenments.txt'
            }
         }
     }
}